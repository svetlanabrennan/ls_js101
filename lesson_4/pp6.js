let arr = [1, 2, 3, 4, 5]
arr.fill(1, 1, 5);

//  .fill returns a modified array filling the array with a indicated value as the
// first argument, start of index to fill the indicated value as the second argument
// and the end index to stop the indicated value with as the third argument. 

// This method is destructive - check mdn. 

// it'll return [1, 1, 1, 1, 1]