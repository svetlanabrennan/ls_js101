/* 
** var foo = ['a', 'b', 'c'];
** foo.length;  // returns 3
** foo[3];      // will this result in an error?
** No there will not be an error because it will return undefined since it can't access
** an element at index 3 since there is no index 3 in this array.
** 
*/